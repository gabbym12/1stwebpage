# Replit setup

This project is a static HTML page. Its Replit workflow serves the repository
root with Python's built-in HTTP server on port 5000:

```bash
python3 -m http.server 5000 --bind 0.0.0.0
```

No package installation or environment secrets are required.